- [ ] Inspect current bulk upload template + import validator/execute code paths
- [ ] Implement normalizer layer before validation (deal_type/property_type/status/booleans/numerics + friendly labels)
- [ ] Add strict parent-child location validation (state_region -> district -> township) with row-level errors
- [ ] Update XLSX template to include dropdowns for district and township; attempt dependent dropdowns if safe
- [ ] Keep CSV support working; status always imported as draft
- [ ] Wire preview and execute to use the same normalization/validation
- [x] Inspect current bulk upload template + import validator/execute code paths
- [ ] Implement normalizer layer before validation (deal_type/property_type/status/booleans/numerics + friendly labels)
- [ ] Add strict parent-child location validation (state_region -> district -> township) with row-level errors
- [ ] Update XLSX template to include dropdowns for district and township; attempt dependent dropdowns if safe
- [ ] Keep CSV support working; status always imported as draft
- [ ] Wire preview and execute to use the same normalization/validation
- [ ] Run typecheck (if available) and fix any TS issues
- [ ] Smoke-test template download + preview + execute with a few sample rows


